<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FAFAFA">

    <!-- Zone du Script (Scrollable) -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="Script :"
        android:textStyle="bold"
        android:textColor="#555"/>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#FFF"
        android:padding="16dp"
        android:layout_margin="10dp"
        android:elevation="2dp">

        <TextView
            android:id="@+id/txtScriptDisplay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Aucun script défini pour cette chronique."
            android:textSize="20sp"
            android:textColor="#000"
            android:lineSpacingExtra="6dp"/>
    </ScrollView>

    <!-- Zone de contrôle Enregistrement -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="20dp"
        android:background="#EEE">

        <Chronometer
            android:id="@+id/chronometer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="50sp"
            android:textColor="#212121"
            android:layout_marginBottom="20dp"/>

        <ImageButton
            android:id="@+id/btnRecordToggle"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:src="@drawable/ic_record"
            android:background="?selectableItemBackgroundBorderless"
            android:scaleType="fitCenter"
            android:contentDescription="Enregistrer"/>
    </LinearLayout>
        
</LinearLayout>